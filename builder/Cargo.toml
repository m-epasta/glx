[package]
name = "builder"
version = "0.1.0"
edition = "2024"

[dependencies]
camino = "1.2.2"
commons = { path = "../commons"  }
serde = { version = "1.0.228", features = ["serde_derive"] }
toml = "1.1.2"
