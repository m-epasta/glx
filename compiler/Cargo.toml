[package]
name = "compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
commons = { path = "../commons" }
camino = "1.2.2"
serde = { version = "1.0", features = ["serde_derive"] }
thiserror = "2.0.18"
toml = "^1.1"

[dev-dependencies]
tempdir = "0.3"
