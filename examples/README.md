# GLX EXAMPLES

This directory contains examples (at base/root level of examples/) but also integrations_tests
under tests/
Those tests are examples that are just shared with the compiler to make sure everything works as expected

## Run tests
To run *compiler* tests, you can use just test into examples/ or or compiler/
