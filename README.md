# GLX COMPILER

The GLX compiler is a rust crate callable by every rust project that can do it. It assumes that it has to process a **SINGLE** file. Why ? The CLI does it already, that is why a lot of frameworks and languages like gleam, divides the compiler into core and cli. 
